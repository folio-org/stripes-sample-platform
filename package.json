{
  "license": "Apache-2.0",
  "scripts": {
    "build": "stripes build stripes.config.js",
    "stripes": "stripes",
    "jason": "stripes dev stripes.config.js",
    "mike": "f=stripes.config.js; test -f $f || f=$f.example; echo Using config $f; stripes dev $f"
  },
  "dependencies": {
    "@folio/stripes-core": "^0.0.9-test",
    "@folio/users": "^0.0.1-test",
    "@folio/items": "^0.0.1-test",
    "@folio/trivial": "^0.0.2-test"
  }
}
