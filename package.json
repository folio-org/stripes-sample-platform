{
  "license": "Apache-2.0",
  "scripts": {
    "build": "stripes build stripes.config.js",
    "stripes": "stripes",
    "start": "stripes dev stripes.config.js",
    "local": "f=stripes.config.js; test -f $f.local && f=$f.local; echo Using config $f; stripes dev $f"
  },
  "dependencies": {
    "@folio/stripes-core": "^1.0.0",
    "@folio/trivial": "^0.0.2-test",
    "@folio/users": "^1.3.0",
    "@folio/items": "^1.2.0",
    "@folio/scan": "^0.3.0",
    "@folio/organization": "^0.1.0"
  }
}
